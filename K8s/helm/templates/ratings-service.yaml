apiVersion: v1
kind: Service
metadata:
  name: ratings
  labels:
    service: ratings
  {{- if .Values.topologyAwareServices }}
  annotations:
    service.kubernetes.io/topology-mode: "Auto"
  {{- end }}
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    service: ratings
