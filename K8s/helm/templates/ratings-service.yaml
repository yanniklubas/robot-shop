apiVersion: v1
kind: Service
metadata:
  name: ratings
  labels:
    service: ratings
spec:
  {{- if .Values.topologyAwareServices }}
  trafficDistribution: PreferClose
  {{- end }}
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    service: ratings
