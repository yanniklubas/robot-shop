apiVersion: v1
kind: Service
metadata:
  labels:
    service: redis
  name: redis
  {{- if .Values.topologyAwareServices }}
  annotations:
    service.kubernetes.io/topology-mode: "Auto"
  {{- end }}
spec:
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  selector:
    service: redis
